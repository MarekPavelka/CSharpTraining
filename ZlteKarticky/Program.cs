using System;

namespace ZlteKarticky
{
    class Program
    {
        static void Main(string[] args)
        {
            // Pokladna.Run(); 1
            // GuessNumber.Run(); 2
            // Vydavac.Run(); 3
            // Factorial.Run(); 4
            // Fibonacci.Run(); 5 <- second hardest
            // GuessNumberSolver.Run(); 6 <- second hardest
            // NacitavanieAut.Run(); 7
            // MenuAuta.Run(); 8 <- hardest
            // ArrayReverse.Run(); 9

            Console.ReadLine();
        }
    }
}
